server:
   port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: movie-info-service
  output:
    ansi:
      enabled: always

logging:
  level:
    root: debug


